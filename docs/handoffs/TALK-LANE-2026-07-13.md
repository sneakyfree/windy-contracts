# Handoff: Windy Talk lane — doctrine-compliance punch list

**Audience:** the Fable instance in the windytalk terminal on Windy 0.
**Authority:** Grant. **Scope: the windytalk repo ONLY.** You are the pattern
donor whose Gen-3 design became ecosystem law — this list finishes YOUR
platform's compliance with that law. You are NOT the doctrine lane: do not
edit windy-contracts (propose PRs there instead), do not touch any other
repo, do not touch windy-cloud / windy-code (a third lane owns those).

## Context in 60 seconds

The Agent Control Doctrine is now law: **ADR-060** in
`github.com/sneakyfree/windy-contracts` (private; clone it). Your
`control.mcp.v1` rev.6 was its reference implementation. The Loom (same
repo) now validates manifests and GENERATES bilingual surfaces —
`uv run python -m loom.validate <manifest>` and
`uv run python -m loom.generate <manifest> <weave.json> <outdir>`. Your
contract already validates with **0 errors**; the warnings it emits are your
migration ramp, and closing that ramp is part of this list. Read, in order:
`AGENT_CONTROL.md` (the law), `schema/control-manifest.v1.schema.json`,
`schema/weave-config.v1.schema.json`, `schema/surfaces.v1.schema.json`,
`docs/PROGRESS.md` (fleet state).

## The punch list (in order; feature branches + PRs in windytalk; local
## gates are the gates — Actions is billing-locked)

1. **Engine-box control surface.** `server/` is an empty stub, so a wedged
   engine box can only be fallen away from, not healed (doctrine §7). Build
   its control surface per your own CONTROL_SURFACE_DESIGN two-layer
   pattern: 13-knob baseline (tri-state `unsupported` where honest), its own
   frozen contract file (`engine.mcp.v1.json` — validate with the Loom
   before freezing), same security wall, doctor-not-in-patient.
2. **Close the v1→v2 ramp on control.mcp.v1** (additive v1.1 PR, per your
   own change-control law): add `doctrine: "ADR-060 v1.0"`, `product:
   "windytalk"`, `class: "desktop"` headers; add the missing `returns`
   schemas the validator warns about (`uv run python -m loom.validate
   contracts/control.mcp.v1.json` in a windy-contracts checkout lists them).
   Same for hands.mcp.v1.
3. **Native-transport shape check (ADR-060 §3.2):** your surface must serve
   `GET /tools` (the manifest's tool list, verbatim) and `POST /invoke
   {name, arguments}` → `{ok, result?|error?}` alongside your existing
   routes. If :8782 already speaks this shape, done; if not, add it — the
   Loom-woven packets and the conformance driver's live gate assume it.
4. **Account/billing knobs** (doctrine §7 — Word has 6; add Talk's
   equivalents to the contract via the same additive v1.1).
5. **`surfaces.json` registration:** on startup write your entry to
   `~/.windy/surfaces.json` per `schema/surfaces.v1.schema.json` (0600,
   remove on clean shutdown, include `token_path` and `pid`). Both the app
   surface and, later, the engine-box surface register.
6. **Weave config:** add `contracts/weave.json` per
   `schema/weave-config.v1.schema.json` (product windy-talk, class desktop,
   your :8782 base, install_token auth with your token paths, package name
   for the future npm packet). Do NOT publish any packet — publishing is
   Grant-gated; the weave config just makes Talk weavable.
7. **Wire the Loom into `make check`:** validation of both contracts must
   run in your local gate (vendor a windy-contracts checkout path or
   `uv run --project ../windy-contracts`). A contract edit that breaks
   validation must fail your build.

## Report back

When the list is done (or blocked), write a short summary for Grant:
what merged (PR links), what the engine-box surface can heal now, any
deltas you propose to the doctrine (as windy-contracts PR suggestions,
never direct edits). If anything here contradicts your frozen contracts'
change-control law, the contract law wins — flag the conflict instead of
improvising.
